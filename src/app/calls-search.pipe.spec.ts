import { CallsSearchPipe } from './calls-search.pipe';

describe('CallsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CallsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
